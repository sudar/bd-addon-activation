{
    "name": "bulk-wp/bulk-delete-addon-activator",
    "description": "Bulk Delete Add-on Activator Library",
    "type": "library",
    "license": "GPL-2.0-or-later",
    "authors": [
        {
            "name": "Sudar Muthu",
            "email": "sudar@sudarmuthu.com"
        }
    ],
    "minimum-stability": "stable",
    "config": {
        "sort-packages": true
    },
    "require": {
        "php": ">=5.6"
    },
    "require-dev": {
        "dealerdirect/phpcodesniffer-composer-installer": "^0.5.0",
        "phpcompatibility/phpcompatibility-wp": "^2.0"
    },
    "scripts": {
        "phpcs": "./vendor/bin/phpcs -p src/ --standard=PHPCompatibilityWP --runtime-set testVersion 5.2"
    }
}
